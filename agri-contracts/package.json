{
  "name": "albaraka-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "عقود ووصولات الآلات الزراعية — معرض البركة لتجارة السيارات الحديثة",
  "scripts": {
    "serve": "npx --yes serve www -l 5173",
    "cap:add:android": "npx cap add android && node scripts/apply-android-extras.mjs",
    "cap:sync": "npx cap sync android && node scripts/apply-android-extras.mjs",
    "android:apk": "cd android && ./gradlew assembleDebug",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/share": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
